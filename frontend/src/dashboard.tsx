import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "Game Management System" />
      <CardContent> A system to manage players, games, animations, colors and lights. < /CardContent>
      </Card>
    );